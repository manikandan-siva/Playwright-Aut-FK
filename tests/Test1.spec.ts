let username:string="mani";
let age:number = 25;
let log:boolean = false;

interface user {
    username : string;
    password : string;
}

const testUser: user ={
    username:"adi",
    password:"ad123"
};